#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <Windows.h>

#define p1

#if defined(p1)
int main() {
    return 0;
}
#endif

#if defined(p2)
int main() {
    return 0;
}
#endif

#if defined(p3)
int main() {
    return 0;
}
#endif

#if defined(p4)
int main() {
    return 0;
}
#endif